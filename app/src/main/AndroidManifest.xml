<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.thetrempiada">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <meta-data android:name="com.google.android.geo.API_KEY"
    android:value="AIzaSyAYHcURWxEotPBmJM9BLVkXpY4Fn8-t-h0"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TheTrempiada">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyB_qsP8AOP_P0MdlPz-48TDaJYjTP3vbjo" />

        <activity android:name=".MainActivity">
        </activity>
        <activity android:name=".AfterLogin"></activity>
        <activity android:name=".DriverMain"></activity>
        <activity android:name=".ActivityEditUser"></activity>
        <activity android:name=".TrempistMain"></activity>
        <activity android:name=".driverActivities.AddTrip"></activity>
        <activity android:name=".mapActivity"></activity>
        <activity android:name=".trempistActivities.ActivitySearchTremp"></activity>
        <activity android:name=".trempistActivities.SearchResults"></activity>
        <activity android:name=".mapActivityForSearch"></activity>
        <activity android:name=".driverActivities.MyTremps"></activity>
        <activity android:name=".driverActivities.EditTremp"></activity>
        <activity android:name=".preMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trempistActivities.MyTremps"></activity>
    </application>

</manifest>